# Skill Atlas（macOS）产品需求文档 PRD

## 1. 产品定位
Skill Atlas 是一个 macOS 原生应用，用于统一管理 Codex 与 Claude Code 的 Skills，支持查看、添加、删除、同步、版本保护，以及按需求快速发现并安装合适 Skill。

目标是把当前手动复制目录、手动比对、手动安装的流程变成可视化、可恢复、可追踪的标准流程。

## 2. 背景与问题
当前使用痛点：
1. Skills 分散在 `~/.codex/skills` 与 `~/.claude/skills`，双端状态不一致。
2. 同名 Skill 内容差异难以发现，误覆盖风险高。
3. 删除操作缺少保护，误操作代价大。
4. 找新 Skill 依赖零散站点和仓库，检索效率低。

## 3. 产品目标
1. 提供双端统一视图，清晰展示差异与健康状态。
2. 提供安全同步机制（可预览、可备份、可恢复）。
3. 提供统一的添加/删除/批量操作能力。
4. 提供“按需求找 Skill”的发现中心，支持一键安装。

## 4. 非目标（v1 不做）
1. 云端账号体系与多设备云同步。
2. 自动执行 Skill 内脚本的沙箱运行器。
3. 团队权限体系（RBAC）。

## 5. 目标用户
1. 同时使用 Codex 与 Claude Code 的个人开发者。
2. 需要稳定维护本地 Skill 资产的 AI 工程实践者。

## 6. 核心使用场景
1. 我想知道双端有哪些 Skill 不一致。
2. 我想把某个 Skill 从 Codex 同步到 Claude。
3. 我想删掉失效 Skill，但希望可恢复。
4. 我有一个任务目标，希望快速找到合适 Skill 并安装。

## 7. 功能需求

### 7.1 统一资产总览（Dashboard）
功能：
1. 扫描并展示两端目录：`~/.codex/skills`、`~/.claude/skills`。
2. 状态标签：仅 Codex、仅 Claude、双端一致、双端冲突。
3. 健康检查：
- 是否存在 `SKILL.md`
- frontmatter 是否包含 `name`、`description`
- 目录结构统计（scripts/references/assets）
4. 支持按名称、状态、更新时间过滤。

验收标准：
1. 首次扫描完成后，列表可展示全部 Skill 与状态。
2. 冲突 Skill 可明确标红并支持进入差异页。

### 7.2 同步中心（Sync）
功能：
1. 单个同步：Codex -> Claude 或 Claude -> Codex。
2. 批量同步：按过滤条件批量执行。
3. 冲突处理：同步前展示 diff，用户选择覆盖方向。
4. 同步模式：
- `symlink` 模式（节省空间，统一源）
- `copy` 模式（独立副本）

验收标准：
1. 任意同步动作都可看到预览差异。
2. 同步完成后状态实时刷新。

### 7.3 安全删除与恢复（Safety）
功能：
1. 删除前展示影响范围（双端路径、关联文件）。
2. 删除前自动创建备份快照。
3. 提供备份列表与一键恢复。
4. 文件写入采用原子写入，避免中断损坏。

验收标准：
1. 每次删除都会生成可追踪备份记录。
2. 可基于备份恢复到删除前状态。

### 7.4 发现中心（Discover）
功能：
1. 输入“需求描述”进行检索（例如：前端重构、PR 评审、论文数据处理）。
2. 聚合多来源结果并排序：
- 本地已安装 Skill
- GitHub 仓库来源（官方/社区）
- Skills 目录站点（可配置）
3. 结果卡片展示：名称、用途摘要、来源、更新时间、热度。
4. 一键安装到目标端：Codex / Claude / 双端。

默认来源（v1）：
1. GitHub：`openai/skills`、`anthropics/skills`、`vercel-labs/skills`
2. skills.sh（目录与热度参考）

验收标准：
1. 输入需求后可返回可安装候选列表。
2. 安装完成后在 Dashboard 可立即看到新 Skill。

### 7.5 设置（Settings）
功能：
1. 路径设置：支持自定义 skill 根目录。
2. 来源管理：启用/禁用来源，配置优先级。
3. 安全策略：默认同步模式、删除确认策略。

## 8. 信息架构
1. Dashboard（总览）
2. Sync（同步）
3. Discover（发现）
4. Backups（备份与恢复）
5. Settings（设置）

## 9. 关键流程

### 9.1 首次启动
1. 自动探测双端 skill 目录。
2. 完成索引扫描。
3. 展示差异摘要与建议动作。

### 9.2 发现并安装
1. 输入需求 -> 检索 -> 预览 Skill -> 选择安装目标 -> 完成安装。

### 9.3 删除并恢复
1. 选择 Skill -> 预览影响 -> 生成备份 -> 执行删除。
2. 在 Backups 页面可选择快照恢复。

## 10. 数据模型（v1）
1. SkillRecord
- id
- name
- side（codex/claude/both）
- path
- hash
- mtime
- healthWarnings[]

2. SourceRecord
- sourceId
- sourceType（github/registry/local）
- enabled
- priority
- trustLevel

3. BackupRecord
- backupId
- operationType
- createdAt
- files[]

4. InstallEvent
- eventId
- skillName
- source
- targetSide
- result
- timestamp

## 11. 非功能需求
1. 本地优先：离线情况下仍可完成管理与同步。
2. 性能：
- 冷启动首次索引 <= 3 秒（中小规模目录）
- 增量刷新 <= 1 秒
3. 稳定性：写入失败不破坏已有文件。
4. 可审计：关键操作有日志与时间戳。

## 12. 技术建议（实现导向）
1. 客户端：SwiftUI + AppKit（文件对比与系统对话框）。
2. 文件层：FileManager + 文件监听（FSEvents）。
3. 索引层：SQLite（缓存 hash、mtime、来源、状态）。
4. 差异与安全层：
- diff 预览
- 原子写入
- 本地备份快照

## 13. 版本规划

### MVP（1~2 周）
1. 双端扫描与状态展示。
2. 单个 Skill 同步。
3. 删除备份与恢复。

### V1（+2 周）
1. 批量同步与冲突处理。
2. 发现中心（多来源检索 + 一键安装）。
3. 来源管理与优先级。

### V1.5
1. 评分模型（质量/风险/兼容性）。
2. 需求到 Skill 的语义推荐优化。

## 14. 风险与应对
1. 来源质量参差：默认展示来源与风险提示。
2. 跨工具兼容差异：安装前标注支持矩阵。
3. 用户误操作：强制预览 + 备份 + 恢复路径。

## 15. 成功指标（v1）
1. 双端不一致 Skill 比例下降 60% 以上。
2. 发现到安装平均时长 < 60 秒。
3. 删除误操作后恢复成功率 100%。
4. 每周活跃用户中，批量同步使用率 > 40%。

